[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "data-models"
version = "0.1.0"
dependencies = [
    "protobuf>=4.21.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["generated*"]
